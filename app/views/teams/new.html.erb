<h1>New team</h1>

<%= form_for(@team) do |f| %>
  <%= errors_for(@team) %>
	<table>
	<%= render :partial => "form", :locals => { :f => f } %>
	</table>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', teams_path %>
