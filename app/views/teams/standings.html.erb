<h1>Team Standings</h1>

<%= link_to 'Back', teams_path %>

<table border="1" style="border-collapse:collapse">
	<tr>
		<td>#</td><td>Name</td><th>Avg</th>
		<% (1..APP_CONFIG["max_matches"]).each do |col| %>
			<td width="25px"><%= col %></td>
		<% end %>
	</tr>
<% @teams.each do |team| -%>
	<tr><%= "<td>#{team.fll_number}</td><td>#{team.team_name}</td>" %>
		<%= "<td align='right'><b>#{team.average_score}</b></td>" %>
	<% team.matches.each do |match| %>
	<%= "<td align='right'>#{match.score}</td>" %>
	<% end %>
	<% if team.matches.length < APP_CONFIG["max_matches"]
		(team.matches.length+1..APP_CONFIG["max_matches"]).each do %>
		<td>&nbsp;</td>
		<% end 
	   end -%>
	</tr>
<% end -%>
</table>